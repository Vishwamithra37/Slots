# Slots
Slot booking Application

 g#to merge branches in git
 git checkout master
 git merge Navya's module 
 #after resolving conflicts between the files 
 git push origin master

 ----resolve conflicts in vscode----
 branch-...-pull-
 git pull origin master
 head to == old code---->delete them
 delete all the >>
 save and commit

 #in terminal 
 git add .
 git status(files modifiedto local branch will shown)
 git commit -m "newcommit"
 git push

 #in git lab
 pullrequest-new pull request-create pull req-view pull request-ready for review-merge request-confirm merge
 check master branch to see the changes/latest commits
    red-previous code
    green-new code
    white-no code

#file structure
     app.py

#latest pull from master branch so that other codes will be seen 
 and you can easily merge requests 




